{
  "name": "heal-demo-app",
  "version": "1.0.0",
  "description": "Deliberately broken app for deploy-doctor heal demo — crashes on Render's ephemeral filesystem",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
